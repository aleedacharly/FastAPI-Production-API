# Production-Grade-REST-API-with-FastAPI
Production-style backend service that exposes a REST API for managing resources (users + items), with authentication, persistence, observability, and deployment readiness.
